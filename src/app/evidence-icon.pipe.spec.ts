import { EvidenceIconPipe } from './evidence-icon.pipe';

describe('EvidenceIconPipe', () => {
  it('create an instance', () => {
    const pipe = new EvidenceIconPipe();
    expect(pipe).toBeTruthy();
  });
});
